# SAM build for the Python function: no dependencies (boto3 ships with the runtime),
# so just copy the sources. Keeps the build free of a local python3.12 requirement.
build-ChatFunctionPy:
	cp -r *.py agents $(ARTIFACTS_DIR)/
